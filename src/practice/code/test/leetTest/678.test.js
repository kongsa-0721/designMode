/**
 * Created by KongSa on 2022/9/16-4:36 PM.
 */
import * as assert from "assert";
import { fn } from "../../src/leetcode/678.js";
it("6789", () => {
  let res = true;
  assert.equal(
    fn("(((((*(((((*((**(((*)*((((**))*)*)))))))))((*(((((**(**)"),
    res
  );
});
